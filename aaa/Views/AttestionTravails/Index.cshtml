@model IEnumerable<aaa.Models.AttestionTravail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dossier)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PeriodeReference)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalaireSoumisCotisation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dossier.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PeriodeReference)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalaireSoumisCotisation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AttestionTravailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AttestionTravailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AttestionTravailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
